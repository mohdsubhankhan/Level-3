<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Twitter</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100 font-sans">
    <header class="bg-white shadow">
        <div class="max-w-4xl mx-auto py-4 px-6">
            <h1 class="text-3xl font-bold text-blue-500">Twitter</h1>
        </div>
    </header>
    <main class="max-w-4xl mx-auto py-6 px-4">
        <div class="mb-6">
            <a href="/twitter/new" class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded">New Tweet</a>
        </div>
        <div class="space-y-4">
            <% tweets.forEach(tweet => { %>
                <div class="bg-white p-4 rounded shadow">
                    <div class="flex justify-between items-center">
                        <h3 class="text-xl font-semibold text-gray-800">@<%= tweet.username %></h3>
                        <div class="space-x-2">
                            <a href="/twitter/<%= tweet._id %>" class="text-blue-500 hover:underline">Show</a>
                            <a href="/twitter/<%= tweet._id %>/update" class="text-green-500 hover:underline">Edit</a>
                            <form action="/twitter/<%= tweet._id %>?_method=DELETE" method="POST" class="inline">
                                <button class="text-red-500 hover:underline" onclick="return confirm('Are you sure?')" type="submit">Delete</button>
                            </form>
                        </div>
                    </div>
                    <p class="mt-2 text-gray-700"><%= tweet.content %></p>
                </div>
            <% }) %>
        </div>
    </main>
</body>

</html>