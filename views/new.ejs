<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Tweet</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 font-sans">
    <header class="bg-white shadow">
        <div class="max-w-4xl mx-auto py-4 px-6">
            <h1 class="text-3xl font-bold text-blue-500">New Tweet</h1>
        </div>
    </header>
    <main class="max-w-4xl mx-auto py-6 px-4">
        <div class="bg-white p-6 rounded shadow">
            <form action="/twitter" method="POST" class="space-y-4">
                <div>
                    <input type="text" name="username" placeholder="Username"
                        class="w-full border border-gray-300 p-2 rounded focus:outline-none focus:border-blue-500">
                </div>
                <div>
                    <textarea name="content" rows="4" placeholder="What's happening?"
                        class="w-full border border-gray-300 p-2 rounded focus:outline-none focus:border-blue-500"></textarea>
                </div>
                <div>
                    <button type="submit"
                        class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded">Tweet</button>
                </div>
            </form>
        </div>
    </main>
</body>
</html>