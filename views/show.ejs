<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Tweet</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 font-sans">
    <header class="bg-white shadow">
        <div class="max-w-4xl mx-auto py-4 px-6">
            <h1 class="text-3xl font-bold text-blue-500">Tweet Details</h1>
        </div>
    </header>
    <main class="max-w-4xl mx-auto py-6 px-4">
        <div class="bg-white p-6 rounded shadow">
            <p class="text-gray-600 mb-2">Tweet ID: <%= tweet._id %></p>
            <h3 class="text-xl font-semibold mb-4 text-gray-800">@<%= tweet.username %></h3>
            <p class="text-gray-700 mb-4"><%= tweet.content %></p>
            <div class="space-x-2">
                <a href="/<%= tweet._id %>/update"
                    class="bg-green-500 hover:bg-green-600 text-white py-2 px-4 rounded">Edit</a>
                <a href="/"
                    class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded">Back</a>
            </div>
        </div>
    </main>
</body>
</html>